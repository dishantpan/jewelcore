spring:
  application:
    name: jewelcore

  datasource:
    url: jdbc:mysql://localhost:3306/jewelcore
    username: root
    password: Dish@7890

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

jwt:
  secret: ${JWT_SECRET:change-this-development-secret-key-please-use-a-long-random-value}

server:
  port: 8080